document.addEventListener('DOMContentLoaded', () => {
    // Adiciona uma classe ao corpo quando a página está pronta para 
    // garantir que todos os estilos foram aplicados antes de qualquer animação
    document.body.classList.add('loaded');
    
    console.log('Codifica Premium: Site de alto nível carregado com sucesso.');

    // Exemplo de uma funcionalidade mais sofisticada: 
    // um "scroll to top" invisível que pode ser adicionado posteriormente.
});
